{
  "name": "usage",
  "scripts": {
    "lint": "eslint --cache --fix .",
    "format": "prettier --cache --write .",
    "test": "vitest run",
    "test:watch": "vitest watch",
    "prisma:gen": "prisma generate",
    "prisma:studio": "prisma studio",
    "prisma:migrate": "prisma migarte dev"
  },
  "devDependencies": {
    "prisma": "^4.5.0"
  },
  "dependencies": {
    "dotenv": "^16.0.3",
    "dotenv-expand": "^9.0.0"
  }
}
